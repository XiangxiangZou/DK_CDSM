# Configuration Location

Deployment configuration files now live in:

```text
configs/deployment/
```

This directory is retained only so older documentation links fail clearly
instead of silently using duplicated configuration files.
