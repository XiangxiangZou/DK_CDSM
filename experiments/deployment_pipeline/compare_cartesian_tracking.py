"""Compare Cartesian IK tracking with the control-ready models."""

from experiments._bootstrap import run_project_script

if __name__ == "__main__":
    run_project_script(
        "archive/deployment_pipeline/run_05_cartesian_ik_tracking_compare.py"
    )
